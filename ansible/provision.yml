---
- hosts: router
  strategy: free
  remote_user: ubuntu
  gather_facts: False
  roles:
      - common
      - router

- hosts: server
  strategy: free
  remote_user: ubuntu
  gather_facts: False
  roles:
      - common
      - router
      - server

